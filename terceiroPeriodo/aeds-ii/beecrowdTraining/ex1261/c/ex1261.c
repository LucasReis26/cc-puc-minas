#include <stdio.h>
#include <stdlib.h>

int main(){
	int quant_palavras,
		quant_descricoes,
		*words = (int *)malloc(quant_palavras * sizeof(int)),
		*values = (int *)malloc(quant_palavras * sizeof(int));


	free(words);
	free(values);

	return 0;
}
